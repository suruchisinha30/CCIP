#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int main() {
    
    int arr[6],i, mul=0,sum=0;
    for(i=1; i<=6; i++)
    {
        cin>>arr[i];
    }
    for(i=1; i<=6; i++)
    {
        if(arr[i]%2 != 0)
        sum= sum + arr[i];
    }
    
    cout<<sum;
    return 0;
}
